import React, {useEffect} from "react";
import useThemeStore from "../../app/stores/useThemeStore";
import {callTo} from "@shared/lib/callTo";

export const CallSupport = () => {
  return (
    <>
      {/*<button className='relative transition top-[-2px] cursor-pointer text-grayText dark:text-grayText-dark
                         hover:text-secondary dark:hover:text-secondary
                         underline underline-offset-4 px-4 text-sm rounded-xl'
              onClick={() => callTo(79318112787, "_blank")}>
        мне нужна помощь
      </button>*/}
    </>
  )

}
